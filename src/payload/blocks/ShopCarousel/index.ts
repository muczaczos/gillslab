import type { Block } from 'payload/types'

export const ShopCarousel: Block = {
  slug: 'shopCarousel',
  fields: [
    {
      name: 'title',
      label: 'Title',
      type: 'text',
    },
  ],
}
