version: "3.8"

services:
  cms:
    build: .
    ports:
      - 3000:3000
    volumes:
      - .:/home/node/app
      - /home/node/app/node_modules
    env_file:
      - .env
    stdin_open: true
    tty: true
